#version 450

#extension GL_EXT_mesh_shader : require

struct Payload {
	uint ID;
};

void _S5(out uvec3  _S6[12])
{
    const uvec3 _S7 = uvec3(1U, 2U, 3U);
    const uvec3 _S8 = uvec3(1U, 2U, 6U);
    const uvec3 _S9 = uvec3(1U, 5U, 6U);
    const uvec3 _S10 = uvec3(5U, 6U, 4U);
    const uvec3 _S11 = uvec3(7U, 4U, 6U);
    const uvec3 _S12 = uvec3(7U, 4U, 3U);
    const uvec3 _S13 = uvec3(4U, 0U, 3U);
    const uvec3 _S14 = uvec3(3U, 2U, 7U);
    const uvec3 _S15 = uvec3(2U, 6U, 7U);
    const uvec3 _S16 = uvec3(0U, 1U, 4U);
    const uvec3 _S17 = uvec3(1U, 5U, 4U);
    _S6[0] = uvec3(0U, 1U, 3U);
    _S6[1] = _S7;
    _S6[2] = _S8;
    _S6[3] = _S9;
    _S6[4] = _S10;
    _S6[5] = _S11;
    _S6[6] = _S12;
    _S6[7] = _S13;
    _S6[8] = _S14;
    _S6[9] = _S15;
    _S6[10] = _S16;
    _S6[11] = _S17;
    return;
}
uvec3 indices_0[12];

void _S18(out vec3  _S19[8])
{
    const vec3 _S20 = vec3(10.0, 0.0, 0.0);
    const vec3 _S21 = vec3(10.0, 0.0, 10.0);
    const vec3 _S22 = vec3(0.0, 0.0, 10.0);
    const vec3 _S23 = vec3(0.0, 10.0, 0.0);
    const vec3 _S24 = vec3(10.0, 10.0, 0.0);
    const vec3 _S25 = vec3(10.0, 10.0, 10.0);
    const vec3 _S26 = vec3(0.0, 10.0, 10.0);
    _S19[0] = vec3(0.0, 0.0, 0.0);
    _S19[1] = _S20;
    _S19[2] = _S21;
    _S19[3] = _S22;
    _S19[4] = _S23;
    _S19[5] = _S24;
    _S19[6] = _S25;
    _S19[7] = _S26;
    return;
}
vec3 cube_0[8];

taskPayloadSharedEXT Payload payloadOut;
void main() {
    uvec3  _S28[12];
    _S5(_S28);
    indices_0 = _S28;

    vec3  _S29[8];
    _S18(_S29);
    cube_0 = _S29;

	Payload payload;
	payload.ID = gl_WorkGroupID.x;

	payloadOut = payload;

	EmitMeshTasksEXT((1U), (1U), (1U));
	return;
}

//void main() {
//	uint ID = gl_WorkGroupID.x;
//	Meshlet meshlet = meshletBuffer.meshlets[ID];
//	payloadOut.vertexOffset   = meshlet.vertexOffset;
//	payloadOut.triangleOffset = meshlet.triangleOffset;
//
//	MeshletBounds bound = meshletBounds.meshletBounds[ID];
//	payloadOut.meshletCol = bound.coneDirection;
//
//	MeshView mesh = meshViewBuffer.meshViews[bound.meshID];
//	payloadOut.materialIndex = mesh.material;
//
//	// Frustum culling against the meshlets sphere.
//	for (int i = 0; i < 6; i++) {
//		vec3 a = vertexBuffer.vertices[i].position.xyz * bound.sphereCenter;
//		if(a.x + a.y + a.z + vertexBuffer.vertices[i].u <= -bound.sphereRadius) return;
//	}
//
//	// Backface culling against the meshlets cone.
//	vec3 coneTip = bound.coneTip;
//	vec3 coneDir = bound.coneDirection;
//	if(dot(normalize(coneTip - camPos.xyz), coneDir) >= bound.coneCutoff && mesh.flags > 0) return;
//
//	EmitMeshTasksEXT(meshlet.triangleCount, 1, 1);
//}